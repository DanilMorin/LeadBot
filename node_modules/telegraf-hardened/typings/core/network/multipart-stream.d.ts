/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
import * as stream from 'stream';
interface Part {
    headers: {
        [key: string]: string;
    };
    body: NodeJS.ReadStream | NodeJS.ReadableStream | Buffer | string;
}
declare class MultipartStream extends stream.Readable {
    private boundary;
    private parts;
    private partIterator?;
    private reading;
    constructor(boundary: string);
    addPart(part: Part): void;
    _iterator(): AsyncGenerator<string | Buffer, void, unknown>;
    _read(): void;
    private readNext;
    static isStream(stream: unknown): stream is {
        pipe: MultipartStream['pipe'];
    };
}
export default MultipartStream;
//# sourceMappingURL=multipart-stream.d.ts.map